# Equipment Tracker 🖥️

Система учета компьютерного оборудования по материальным ценностям (МЦ).

## 🚀 Возможности

- 📊 Учет оборудования с МЦ номерами
- 👥 Управление сотрудниками и отделами
- 🔍 Поиск по МЦ, сотрудникам, типам оборудования
- 📝 История выдачи оборудования
- 📁 Импорт/экспорт данных в Excel
- 🎯 Фильтрация по статусам, типам, отделам
- 📱 Адаптивный интерфейс (Bootstrap 5)

## 🛠 Технологии

- Python 3.13
- Django 6.0
- SQLite
- Bootstrap 5
- Pandas (для работы с Excel)

## 📦 Установка (разработка)

### Windows (локальная разработка)


# 1. Клонировать репозиторий
git clone https://github.com/Niyaz0912/equipment_tracker.git
cd equipment_tracker

# 2. Создать виртуальное окружение
python -m venv venv

# 3. Активировать окружение
venv\Scripts\activate

# 4. Установить зависимости
pip install -r requirements.txt

# 5. Применить миграции
python manage.py migrate

# 6. Создать суперпользователя
python manage.py createsuperuser

# 7. Запустить сервер
python manage.py runserver

# 8. Открыть в браузере
# Приложение: http://127.0.0.1:8000/
# Админка: http://127.0.0.1:8000/admin/


Linux (серверное развертывание)
bash
# 1. Подключиться к серверу
ssh user@server_ip

# 2. Установить Python и зависимости системы
sudo apt update
sudo apt install python3-pip python3-venv git

# 3. Создать папку для проекта
sudo mkdir -p /var/www/html/tracker
sudo chown -R $USER:$USER /var/www/html/tracker
cd /var/www/html/tracker

# 4. Клонировать проект
git clone https://github.com/Niyaz0912/equipment_tracker.git .

# 5. Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# 6. Установить зависимости
pip install -r requirements.txt

# 7. Настроить базу данных и статику
python manage.py migrate
python manage.py collectstatic --noinput

# 8. Создать суперпользователя
python manage.py createsuperuser

# 9. Запустить сервер (в фоне)
nohup python manage.py runserver 0.0.0.0:8000 > server.log 2>&1 &

# 10. Проверить работу
curl http://localhost:8000

🔧 Быстрое развертывание
Если нужно переустановить на сервере:
bash
# На сервере (Ubuntu)
cd /var/www/html
sudo rm -rf tracker
sudo git clone https://github.com/Niyaz0912/equipment_tracker.git tracker
sudo chown -R $USER:$USER tracker
cd tracker

python3 -m venv venv
source venv/bin/activate

pip install django pandas openpyxl pillow django-import-export

python manage.py migrate
python manage.py loaddata fixtures/employees.json fixtures/equipments.json
python manage.py createsuperuser

# Запуск в фоне
nohup python manage.py runserver 0.0.0.0:8000 > server.log 2>&1 &
Для работы в фоне (серверные команды):
bash
# Запуск в фоне с логированием
nohup python manage.py runserver 0.0.0.0:8000 > server.log 2>&1 &

# Проверка запущенных процессов
ps aux | grep runserver

# Остановка сервера
pkill -f runserver

# Просмотр логов
tail -f server.log
📁 Фикстуры данных
В папке fixtures/ содержатся готовые данные для быстрого старта:

Файл	Записи	Размер	Описание
employees.json	67	16 KB	Отделы и сотрудники
equipments.json	243	100 KB	Оборудование с МЦ номерами
all_data.json	Все данные	249 KB	Полный дамп системы
Загрузка фикстур:

# Загрузить основные данные
python manage.py loaddata fixtures/employees.json fixtures/equipments.json

# Загрузить всё
python manage.py loaddata fixtures/all_data.json
⚙️ Настройка сети
Добавить в config/settings.py:
python
# Разрешенные хосты для доступа по сети
ALLOWED_HOSTS = ['localhost', '127.0.0.1', '192.168.10.52', 'ваш_сервер_ip']

# Для разработки (разрешить все)
ALLOWED_HOSTS = ['*']
Открыть порт на сервере:
bash
# Для Ubuntu с ufw
sudo ufw allow 8000
sudo ufw enable

# Проверить открытые порты
sudo netstat -tulpn | grep :8000
📊 Импорт/Экспорт данных
Импорт из Excel:

Перейти: http://localhost:8000/equipment/import/

Скачать шаблон Excel

Заполнить данные по образцу

Загрузить файл

Экспорт в Excel:

Нажать кнопку "Экспорт" в навигационной панели

Файл сохранит текущие фильтры

Формат: Excel (.xlsx)

🏗️ Структура проекта
text
equipment_tracker/
├── config/              # Настройки Django
├── equipments/         # Приложение для оборудования
├── employees/          # Приложение для сотрудников  
├── history/            # Приложение для истории выдачи
├── fixtures/           # Готовые данные
├── templates/          # Шаблоны (Bootstrap 5)
├── static/            # Статические файлы
├── media/             # Загружаемые файлы
├── import_export/     # Excel файлы
├── requirements.txt   # Зависимости Python
└── manage.py          # Управление Django

🔍 Основные команды

# Создать резервную копию данных
python manage.py dumpdata --indent 2 > backup_$(date +%Y%m%d_%H%M%S).json

# Создать миграции
python manage.py makemigrations

# Применить миграции
python manage.py migrate

# Проверить состояние базы данных
python manage.py check

# Очистить базу данных
python manage.py flush --no-input

# Создать фикстуры для переноса
## 🛠 Утилита для фикстур

# Создать фикстуры для переноса
python fixture_tool.py create

# Настроить новое развертывание
python fixture_tool.py setup

# Создать резервную копию
python fixture_tool.py backup --name "перед_обновлением"
Основные функции:

✅ Автоматическая конвертация в UTF-8

✅ Создание фикстур для выбранных приложений

✅ Загрузка фикстур с очисткой базы

✅ Резервное копирование всей БД

✅ Настройка нового развертывания



## 🎯 **Что делает утилита:**

1. **Кодировка UTF-8** — автоматически проверяет и конвертирует
2. **Умное создание** — можно выбрать конкретные приложения
3. **Безопасная загрузка** — с опцией очистки базы
4. **Резервное копирование** — создает timestamp-бэкапы
5. **Настройка развертывания** — полный цикл миграций + загрузка фикстур
